-r requirements.txt
pytest>=8.0
pytest-asyncio>=0.23
pyflakes>=3.0
# F821 (обращение к несуществующему имени) — тот класс ошибок, что уже
# ронял бота в проде: тест на него есть, а инструмента в окружении не было.
ruff>=0.6
